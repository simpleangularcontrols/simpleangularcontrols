import { Input, EventEmitter, Output, TemplateRef, Component } from '@angular/core';
// import { NgGridColumnCommon } from 'projects/exanic-controls-common/src/public_api';
import { NgGridColumnCommon } from '@jnetwork/jngcontrols-common';

@Component({
  selector: 'ngGridColumn,[ngGridColumn]',
  templateUrl: './gridcolumn.html',
})
export class NgGridColumn extends NgGridColumnCommon {
    
}
