# General information

Bootstrap 3 has no implementation for the implementation of a drop zone by default. Therefore, the styles for this component must be individually adapted in the application depending on use. The library implements markup that only works with custom styles.

However, there are selectors that can be used for customizing. These are the following CSS classes:

- dropzone
- content
- upload-component
